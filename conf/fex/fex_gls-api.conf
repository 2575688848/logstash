input{
    kafka{
        bootstrap_servers => "10.20.0.19:9092,10.20.0.20:9092,10.20.0.22:9092"
        group_id => "fex_gls-api"
        topics => ["fex_gls-api"]
        consumer_threads => 16
	codec => json {
                 charset => "UTF-8"
        }
    }
}

output {
    elasticsearch {
                index => "fex_gls-api_%{+YYYY.MM}"
                hosts => ["http://localhost:9200"]
    }
}
