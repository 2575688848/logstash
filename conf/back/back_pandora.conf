input{
    kafka{
        bootstrap_servers => "10.20.0.19:9092,10.20.0.20:9092,10.20.0.22:9092"
        group_id => "back_pandora"
        topics => ["back_pandora"]
        consumer_threads => 16
	codec => json {
                 charset => "UTF-8"
        }
    }
}

output {
    elasticsearch {
                index => "back_pandora_%{+YYYY.MM}"
                hosts => ["http://localhost:9200"]
    }
}
